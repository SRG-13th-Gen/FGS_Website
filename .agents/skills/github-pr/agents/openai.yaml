interface:
  display_name: "GitHub PR"
  short_description: "Prepare scoped commits and staging-based pull requests"
  default_prompt: "Use $github-pr to prepare the current change and open a pull request into staging."

# Hostinger deployment and recovery

Status 2026-09-26: local checks and the independent preview's public Node deployment passed. The root WordPress website has **not** been removed. The original files/database backups were downloaded and checked; the independent CMS copy, Editor integration account, seven seeded sections, and native preview cache refresh are working. WordPress core, all available plugins, and the active child theme's parent were updated on the CMS copy. Google OAuth, admin writes, and full preview/live smoke tests remain pending.

## Target topology

- `flordegraceschoolinc.com`: public Next.js Node 24 website, from the reviewed `main` commit.
- `cms.flordegraceschoolinc.com`: independent WordPress website and separate database copied from the original. Native school editing and the public REST/media source live here.
- `preview.flordegraceschoolinc.com`: independent Next.js Node 24 website, from the same candidate commit. Set `SITE_INDEXABLE=false`.
- All three use HTTPS. The root WordPress installation remains in place until the backup, CMS copy, and preview gates below pass.

Hostinger Business hosting and its Node 24/Next.js support were checked through the installed Hostinger MCP and [Hostinger's Node.js guide](https://www.hostinger.com/support/how-to-deploy-a-nodejs-website-in-hostinger/) on 2026-09-26. The root homepage returns HTTP 503 while public WordPress REST reads return 200. The independent preview website has active SSL. Hostinger runs Node `24.6.0`; the first build rejected the former `>=24.13.0` engine declaration. The second build failed when Turbopack tried to start a CSS worker process. Commit `a8c7f02` changed the build to supported `next build --webpack`; preview build `01a0d97a-3528-701c-9b5c-9c70c83ddf1d` completed with Node 24 and pnpm 10.30.2. Public route/image checks passed. Cache persistence across restarts or multiple workers remains unverified.

## Release gates

1. In hPanel, generate **files and database** backups for the root WordPress website, download both, and verify archive integrity and that the database export and `wp-content/uploads` are present. Record backup date and safe storage location outside Git. [Hostinger backup steps](https://www.hostinger.com/support/5981435-how-to-download-backups-at-hostinger/).
2. Create `cms.flordegraceschoolinc.com` as an **independent website** on the Business order. Use hPanel **Copy Website** from the original root to that destination. Verify the destination has a separate database, its own files and media, correct WordPress home/site URLs, HTTPS, and REST reads. The source has an active Slim Maintenance Mode plugin that may also cause a 503 on the copy; assess/disable it on the **copy** when checking its front end, leaving the root untouched. In the CMS WordPress admin, enable **Settings → Reading → Discourage search engines from indexing this site**; confirm the CMS homepage has `noindex` while published REST reads still work. Never create the CMS as a disposable child directory of the root website. [Hostinger Copy Website](https://www.hostinger.com/support/6601521-how-to-copy-a-wordpress-website-to-another-domain-name-in-hostinger/), [WordPress Reading setting](https://wordpress.org/documentation/article/settings-reading-screen/).
3. On the CMS copy, install `wordpress/mu-plugins/fgs-site-content.php`, create a dedicated WordPress **Editor** integration account and Application Password, set the webhook URL/secret in a private server-side PHP config loaded by WordPress, and run `pnpm wp:seed-content` against the CMS copy. For hosted seeding, put `WORDPRESS_URL`, `WORDPRESS_USERNAME`, and `WORDPRESS_APPLICATION_PASSWORD` in ignored `.env.cms.local`, then set `FGS_ENV_FILE=.env.cms.local` for the command. The default remains `.env.local` for local development. This script seeds seven sections without overwriting an existing structured field. Apply the [launch content selection](CONTENT_SELECTION.md) to the section fields and public article categories (`clubs`, `events`, `announcements`). Keep all copied old posts/pages/media. Update/test WordPress core and required plugins on the copy only.
4. Configure the independent preview Node website using Node 24, pnpm, application type Next.js, `pnpm build`, and the exact reviewed source archive. Supply the **complete** server-side environment variable set when calling Hostinger's replace-environment API. Required settings: `WORDPRESS_URL=https://cms.flordegraceschoolinc.com`, `WORDPRESS_USERNAME`, `WORDPRESS_APPLICATION_PASSWORD`, `REVALIDATION_SECRET`, `NEXTAUTH_URL=https://preview.flordegraceschoolinc.com`, `NEXTAUTH_SECRET`, `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, `ADMIN_ALLOWED_EMAILS`, `SITE_INDEXABLE=false`. Never commit or print secret values. Google must list `https://preview.flordegraceschoolinc.com/api/auth/callback/google` as an authorized redirect URI.
5. Confirm public sections, images, published/missing article behavior, metadata/noindex, HTTPS, CMS REST, authorized and denied Google sign-in, admin section/article reads and writes, webhook delivery, cache refresh, and runtime logs on preview. `pnpm verify`, browser smoke tests, and targeted auth/CMS/webhook tests must pass for the candidate commit. The school OAuth client and exact first administrator list must be supplied securely before this gate can pass.

   Current partial preview result: `https://preview.flordegraceschoolinc.com/` returned 200; after category curation `/news/test-post` and a missing article returned 404; `/admin` redirected to login. Local and CMS images through `/_next/image` returned 200 after the CMS-source rebuild. `/robots.txt` disallows all crawling and `/sitemap.xml` is empty. The webhook returned 401 without authentication, 400 for malformed input, and 200 for a valid authenticated media event. A native CMS section edit and its restoration propagated to preview. SSL is active with HTTPS redirect; initial runtime logs contained startup messages and no request errors from those checks. Its admin stays closed because Google credentials/list were intentionally skipped. These checks do **not** pass the full gate.

6. Merge the reviewed candidate through `staging` to `main`. **Only after gates 1–5 pass**, remove the old root WordPress website in Hostinger, recreate the root as an independent Node.js website, and deploy the exact merged `main` commit. The destructive remove action loses the old root files/database/configuration; the verified backup and CMS copy are recovery dependencies. Set the same complete environment set with `NEXTAUTH_URL=https://flordegraceschoolinc.com` and `SITE_INDEXABLE=true`; Google must also list `https://flordegraceschoolinc.com/api/auth/callback/google`. Configure SSL and repeat every smoke test, including `/robots.txt` and `/sitemap.xml`.

## Cache events

The CMS must-use plugin sends JSON to `POST /api/revalidate` with `X-FGS-Revalidation-Secret`. It uses `FGS_REVALIDATION_URL` and `FGS_REVALIDATION_SECRET` constants defined privately in `wp-content/mu-plugins/00-fgs-revalidation-config.php` on the CMS copy; this secret-bearing file is **not** in Git. The endpoint accepts only post/page/category/media event shapes, derives tags and routes server-side, and expires data immediately. Article old and new slugs are both invalidated. A failed webhook does not fail an editorial save; the public article, category, media, and section reads retain a 60-second fallback. After cutover, point the CMS webhook to the live root and preserve the same secret in the live Node environment. Repeated events are harmless. See [data contracts](DATA_API_CONTRACTS.md).

## Recovery

- During first root cutover failure, stop further deployment and restore the original root WordPress website from the verified files/database backup in hPanel. Keep the working CMS copy intact. Recheck DNS/SSL and the public homepage.
- For later Node releases, redeploy the last known good reviewed archive/commit and restore its full environment set. Do not roll back WordPress content with an application rollback.
- Keep the verified original backup and independent CMS copy through launch acceptance. Record the deployment commit, backup identity/location, site IDs, environment key names, deployment IDs, smoke results, and rollback outcome in a private operations record without secrets.

The installed Hostinger MCP covers account, website, files, SSL, Node deployment, runtime logs, and environment operations. It does **not** expose backup export or WordPress Copy Website, so those steps require signed-in hPanel. Hostinger's environment endpoint replaces the entire variable set; masked values returned by its list operation cannot be reused as values.

## Staging preview automation

The owner requested a `staging` merge to update the existing Node preview automatically. The prepared [GitHub Actions workflow and setup guide](CI_CD.md) use SSH only to transfer a source ZIP, then Hostinger's API to run the managed build. The GitHub environment has the required key names; branch restrictions and protection remain unconfigured, and no hosted run has succeeded. It does not target the original root WordPress site, the CMS copy, or the future production Node site.

## 2026-09-26 recovery assets and CMS copy

- The original root backup selected in hPanel was displayed as `2026-09-26 00:52` local time. Downloaded archives are in the ignored local `backups/` directory: `u414393871.20260925165259.tar.gz` (files) and `u414393871_FXEn2.20260925165259.sql.gz` (database). SHA-256-matched copies are stored outside the repository at `C:\Users\MaChew\Documents\FGS Website Backups\2026-09-26`. Both archives streamed to completion; the files archive contained 79,738 entries including `wp-config.php`, `wp-content/uploads`, plugins, and themes. The 64,140,960-byte decompressed SQL included WordPress posts and options tables. Archive readability was checked, but a full restore drill has not been run.
- `cms.flordegraceschoolinc.com` was created as an independent website on Business order `1007128386`. hPanel Copy Website showed **Completed** from `flordegraceschoolinc.com` to that destination. Hostinger lists it as a WordPress website with database `u414393871_zMjpK`, distinct from the source database `u414393871_FXEn2`. Its REST API reports `url` and `home` on the CMS domain, published post links point there, an uploaded image returned 200, and SSL/HTTPS redirect are active. The copied Slim Maintenance Mode plugin was deactivated on the CMS copy; its homepage now returns 200. The original root homepage remains at 503.
- `wordpress/mu-plugins/fgs-site-content.php` was uploaded to the CMS copy. A public WordPress page REST response includes `fgs_section_data`, confirming the must-use plugin loaded. The private CMS `00-fgs-revalidation-config.php` supplies the preview webhook URL and shared secret; its direct HTTP URL executes without disclosing content. A temporary contact-section edit appeared on preview and its restoration propagated shortly after, exercising the native event path.
- Hostinger Tools had disabled WordPress Application Passwords on the copy. The user enabled them on the CMS only, created `fgs-site-integration` as an Editor, and saved its Application Password in ignored `.env.cms.local`. Authenticated `users/me?context=edit` returned that Editor account. `pnpm wp:seed-content` created all seven section pages and their structured fields; the public REST API returned valid JSON for each. The preview's server-only environment now has the CMS URL and integration credentials.
- The `clubs`, `events`, and `announcements` categories now exist on the CMS with zero posts. The empty `test-post` was moved from `clubs` to Uncategorized without deleting it. Preview returned the truthful empty News state and `/news/test-post` returned 404. The “Summer Class 2026” post remains in the CMS archive because its February 2026 publication date precedes the May 2026 event it describes and its copy needs review.
- The first preview build was made before `WORDPRESS_URL` pointed to the CMS, so its build-time Next.js image allowlist still named the old root host after a runtime environment update. The exact reviewed `staging` merge commit `3fe9413f71c97d7758f15e169289ae0b29938804` was rebuilt on preview with the CMS environment as build `01a0de33-d349-729b-b2be-b4d3e4b2c7c3` (completed). A CMS image through `/_next/image` then returned 200. Rebuild whenever `WORDPRESS_URL` changes host; an app restart alone does not recalculate `next.config.ts`.
- The operator reported creating a fresh CMS backup before updating WordPress core; its hPanel backup record has not been independently inspected. The operator then supplied the WordPress 7.1.2 welcome screen from the CMS update. Afterward, the CMS homepage, REST index, and seeded Hero page returned 200, with `fgs_section_data` still present. The dedicated integration Application Password still authenticated as an Editor. Preview homepage returned 200, the curated empty article returned 404, and the old root homepage remained at 503. Plugin updates and their checks are next.
- The operator updated all 14 listed plugins in small batches on the CMS copy, then supplied an Updates screen stating the plugins were up to date. After each batch, the CMS homepage, seeded Hero REST field, Editor Application Password, and preview homepage returned successfully; a temporary HTTP 503 during an in-progress batch was WordPress maintenance mode and cleared after completion. The operator then updated the Hello Elementor parent of the active child theme. All seven section REST fields and a preview-optimized CMS image returned 200 afterward. A visual check found that WordPress had automatically added the seven published section pages to its own menu. Must-use plugin version 1.1.1 was uploaded to the CMS copy and confirmed present through Hostinger's file read API; it filters these internal pages from WordPress navigation while keeping their REST reads public. The normal CMS homepage returned 200 with zero section-page menu links, a screenshot showed only the original navigation, and the Editor Application Password still worked. The seven other theme updates shown in hPanel are for themes not used by the active child theme and were not part of this compatibility check.
- The operator later reported updating the seven remaining inactive themes. The CMS homepage returned 200 with the original navigation, the seeded Hero REST field remained public, the Editor Application Password still authenticated, and preview returned 200. The operator enabled the WordPress Reading setting to discourage indexing on the CMS copy. The homepage then emitted `<meta name='robots' content='noindex, nofollow' />`; `/wp-sitemap.xml` returned 404; the published section REST read and Editor credential remained available. `robots.txt` still contained a sitemap link, but that URL returned 404. Desktop and mobile full-page preview screenshots rendered the selected content, including honest empty News and Gallery states. The original root still returned 503 and was not modified.
